apiVersion: v1
kind: Pod
metadata: 
    name: nginx
    namespace: roboshop  
    labels:
        name: nginx
        project: roboshop
        environment: dev
spec: 
    containers:
    - name: nginx
      image: nginx    
      ports:
      - container_port: 80